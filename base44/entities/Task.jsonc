{
  "name": "Task",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Task name"
    },
    "description": {
      "type": "string",
      "description": "Detailed description"
    },
    "project_id": {
      "type": "string",
      "description": "Reference to Project"
    },
    "category_id": {
      "type": "string",
      "description": "Reference to TaskCategory"
    },
    "assigned_team_member_id": {
      "type": "string",
      "description": "Reference to TeamMember"
    },
    "status_id": {
      "type": "string",
      "description": "Reference to StatusList"
    },
    "kanban_bucket_id": {
      "type": "string",
      "description": "Reference to ProjectKanbanBucket for manual Kanban organization"
    },
    "is_priority": {
      "type": "boolean",
      "default": false,
      "description": "Flag indicating if this task is marked as priority"
    },
    "priority_set_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when priority was set. Cleared when priority is removed."
    },
    "start_date": {
      "type": "string",
      "format": "date",
      "description": "Task start date"
    },
    "due_date": {
      "type": "string",
      "format": "date",
      "description": "Task due date"
    },
    "completed_date": {
      "type": "string",
      "format": "date-time",
      "description": "Date and time when task was marked as completed"
    },
    "estimated_hours": {
      "type": "number",
      "description": "Estimated time in decimal hours (e.g. 1.5 = 1h30m)"
    },
    "actual_hours": {
      "type": "number",
      "description": "Actual time spent in decimal hours, entered on completion"
    },
    "completed_by_user_id": {
      "type": "string",
      "description": "Reference to TeamMember who completed the task"
    },
    "sort_order": {
      "type": "number",
      "default": 0,
      "description": "Display order"
    },
    "dependencies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Array of Task IDs this task depends on"
    },
    "external_hours_ref": {
      "type": "string",
      "description": "Placeholder for imported QuickBooks line"
    }
  },
  "required": [
    "name",
    "project_id"
  ]
}